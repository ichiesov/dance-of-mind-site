# Dance of Mind Backend

Backend API для авторизации через Telegram бота.

## Возможности

- 🔐 Авторизация через Telegram бота
- 📱 Поддержка номеров телефонов
- 🔑 JWT токены (access + refresh)
- 💾 Хранение данных в Supabase
- ⚡ FastAPI для высокой производительности
- 🤖 Интеграция с Telegram Bot API

## Требования

- Python 3.11+
- PostgreSQL (через Supabase)
- Telegram Bot Token

## Установка

1. Установите зависимости:

```bash
pip install -r requirements.txt
```

2. Скопируйте `.env.example` в `.env` и заполните переменные:

```bash
cp .env .env
```

3. Создайте таблицы в Supabase, выполнив SQL из `src/database/schema.sql`

4. Создайте Telegram бота через [@BotFather](https://t.me/botfather) и получите токен

## Настройка

### Переменные окружения

Основные переменные в `.env`:

- `TELEGRAM_BOT_TOKEN` - токен Telegram бота
- `SUPABASE_URL` - URL вашего Supabase проекта
- `SUPABASE_KEY` - Anon key от Supabase
- `SUPABASE_SERVICE_KEY` - Service role key от Supabase
- `JWT_SECRET_KEY` - секретный ключ для JWT (сгенерируйте надежный)
- `ALLOWED_ORIGINS` - разрешенные CORS origins (фронтенд URL)

### База данных

Выполните SQL из `src/database/schema.sql` в Supabase SQL Editor для создания таблиц:

- `users` - пользователи
- `auth_sessions` - сессии авторизации

## Запуск

### Режим разработки

```bash
python main.py
```

Сервер запустится на `http://localhost:8000`

### Режим production

```bash
uvicorn main:app --host 0.0.0.0 --port 8000 --workers 4
```

## API Endpoints

### POST `/api/auth/init`

Инициировать процесс авторизации

**Request:**
```json
{
  "phone_number": "+1234567890"
}
```

**Response:**
```json
{
  "session_id": "uuid",
  "expires_in": 300
}
```

### GET `/api/auth/status/{session_id}`

Проверить статус авторизации (polling endpoint для фронтенда)

**Response:**
```json
{
  "status": "approved",
  "tokens": {
    "access_token": "jwt_token",
    "refresh_token": "jwt_token",
    "token_type": "bearer",
    "expires_in": 1800
  }
}
```

Возможные статусы: `pending`, `approved`, `rejected`, `expired`

### POST `/api/auth/refresh`

Обновить access token используя refresh token

**Request:**
```json
{
  "refresh_token": "jwt_token"
}
```

### GET `/api/auth/me`

Получить информацию о текущем пользователе

**Headers:**
```
Authorization: Bearer {access_token}
```

## Процесс авторизации

1. **Фронтенд** → POST `/api/auth/init` с номером телефона
2. **Бэкенд** → Создает auth session и отправляет уведомление в Telegram (если пользователь уже привязан)
3. **Фронтенд** → Начинает polling `/api/auth/status/{session_id}` каждые 2-3 секунды
4. **Пользователь** → Открывает Telegram бота, нажимает "Авторизоваться"
5. **Бэкенд** → Обновляет статус сессии на `approved`
6. **Фронтенд** → Получает токены при следующем polling запросе
7. **Фронтенд** → Использует access token для защищенных запросов

## Структура проекта

```
server/
├── main.py                 # Точка входа
├── requirements.txt        # Python зависимости
├── .env.example           # Пример конфигурации
├── src/
│   ├── api/               # FastAPI endpoints
│   │   └── auth.py        # Auth endpoints
│   ├── bot/               # Telegram bot
│   │   └── telegram_bot.py
│   ├── config/            # Конфигурация
│   │   └── settings.py
│   ├── database/          # БД
│   │   ├── schema.sql     # SQL схема
│   │   └── supabase_client.py
│   ├── models/            # Pydantic модели
│   │   ├── auth.py
│   │   └── user.py
│   └── services/          # Бизнес-логика
│       ├── auth_service.py
│       ├── jwt_service.py
│       └── user_service.py
```

## Безопасность

- JWT токены подписаны секретным ключом
- Access tokens короткоживущие (30 минут по умолчанию)
- Refresh tokens долгоживущие (7 дней по умолчанию)
- Сессии авторизации истекают через 5 минут
- CORS настроен для защиты от несанкционированных запросов
- Row Level Security в Supabase

## Логирование

Все важные события логируются:
- Создание/одобрение сессий авторизации
- Генерация токенов
- Ошибки при работе с API и ботом

## Разработка

### Добавление новых endpoints

1. Создайте router в `src/api/`
2. Добавьте router в `main.py`
3. Используйте существующие сервисы или создайте новые в `src/services/`

### Тестирование

Рекомендуется добавить тесты используя pytest:

```bash
pip install pytest pytest-asyncio httpx
```

## Возможные улучшения

- [ ] Добавить rate limiting
- [ ] Добавить кэширование (Redis)
- [ ] Добавить метрики и мониторинг
- [ ] Добавить webhook вместо polling
- [ ] Добавить дополнительные методы аутентификации
- [ ] Добавить email уведомления

## Поддержка

При возникновении проблем проверьте:

1. Правильность переменных окружения в `.env`
2. Доступность Supabase
3. Валидность Telegram bot token
4. Логи приложения

## Лицензия

Private project
